{
  "name": "vaper-tg-bot-copy",
  "version": "1.0.0",
  "description": "Independent Telegram bot copy prepared for Railway deployment",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "npx prisma db push --accept-data-loss --skip-generate && node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Build complete'",
    "test": "echo 'No tests'",
    "seed": "node server.js --seed"
  },
  "keywords": [
    "telegram",
    "miniapp",
    "ecommerce"
  ],
  "author": "Independent Bot Copy",
  "license": "MIT",
  "dependencies": {
    "@prisma/client": "^5.8.0",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.11.3",
    "prisma": "^5.8.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
